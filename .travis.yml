language: c

compiler:
  - clang
  - gcc

before_script:
  - sudo apt-get install -qq python-dev liblua5.2-dev libpolarssl-dev

script: make

notifications:
  email:
    recipients:
      - proxenet@pyc.li

    on_success: always
    on_failure: always
